# transactions

## 啟動方式

1. 開啟這個資料夾。
2. 執行後端服務：

   ```powershell
   python server.py
   ```

3. 在瀏覽器打開：

   ```text
   http://127.0.0.1:8000
   ```

請用上面的網址開啟，不要直接雙擊 `index.html`，這樣前端才連得到後端和 `transactions_i.db`。
